'use strict';

describe('myApp.nflTeam module', function() {

  beforeEach(module('myApp.nflTeam'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('NflTeamCtrl');
      expect(nflTeamCtrl).toBeDefined();
    }));

  });
});